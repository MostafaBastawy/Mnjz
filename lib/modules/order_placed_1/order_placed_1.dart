import 'package:flutter/material.dart';

class OrderPlaced3 extends StatelessWidget {
  const OrderPlaced3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
