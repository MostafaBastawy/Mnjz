import 'package:flutter/material.dart';

class OrderPlaced4 extends StatelessWidget {
  const OrderPlaced4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
